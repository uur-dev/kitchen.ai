server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${GITHUB_CONFIG_URL}
          default-label: main
          clone-on-start: true
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_SECRET}