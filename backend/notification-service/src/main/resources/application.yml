server:
  port: 0

spring:
  application:
    name: notification-service
  profiles:
    active: rabbitmq,firebase
  config:
    import: "optional:configserver:http://localhost:8888"


  mongodb:
    uri: mongodb://${MONGO_USER}:${MONGO_PASSWORD}@localhost:27017/notification-kitchen-ai?authSource=admin
