server:
  port: 0

spring:
  application:
    name: notification-service
  profiles:
    active: rabbitmq,firebase
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}"


  mongodb:
    uri: mongodb://${MONGO_USER}:${MONGO_PASSWORD}@${MONGO_HOST:localhost}:${MONGO_PORT:27017}/notification-kitchen-ai?authSource=admin